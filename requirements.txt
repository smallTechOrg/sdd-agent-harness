# DataChat — pinned to CURRENT versions verified on PyPI at build time (2026-06-19).
# Switching the LLM provider is a config change (APP_LLM_PROVIDER/_MODEL), not a code change.
langgraph==1.2.6
langchain==1.3.10
langchain-core==1.4.8
langchain-google-genai==4.2.5
langgraph-checkpoint-sqlite==3.1.0      # multi-turn checkpointer (Phase 3); harmless until then
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32                 # file uploads
duckdb==1.5.4                            # analytical store; read_only=True for the agent
plotly==6.1.2                            # chart spec generation (generate_chart_spec tool)
SQLAlchemy[asyncio]==2.0.51
aiosqlite==0.22.1
pydantic-settings==2.14.1
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
# tests
pytest==9.1.0
pytest-asyncio==1.4.0
httpx==0.28.1
